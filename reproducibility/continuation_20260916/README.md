# Preserved September 16 continuation source and tests

This folder is a dated source snapshot inside the existing AIA repository, not a second repository. Source scripts are copied unchanged from the researcher's Cloud Shell home and their SHA-256 hashes are recorded. Original test sources and original test logs are retained from the earlier delivered packages; this copier does not rerun them or certify a new CI environment.

Do not run all the research scripts merely to review this snapshot. Some modules retain home-directory/dated-run defaults and runtime helper-hash checks. They require the original recorded input layout (or supported explicit path arguments), not just a clone. The current working originals remain in place. Review each original_package_notes README before a deliberate replay. The notes retain historical status as written, so the dated public decisions and status records explain later amendments; the combined internal proposal remains private.

The nested tests/test_model_integration.py layout is preserved because that test resolves its module from its parent directory. Other tests live beside their modules, matching their original layouts. To run a specific test deliberately, use its documented environment; no scientific source fetch should occur in the synthetic tests.

See ../../docs/AIA_START_HERE.md for current status, scientific qualifications, scope, and artifact locations.

## Publication scope

The self-contained documentation copier is intentionally not included: it embeds an internal master proposal withheld from public release. The other 15 research source scripts and their test/evidence snapshots retain the reviewed staged bytes. Original scientific run completion markers remain unchanged and describe those original runs, not the smaller subset of artifacts in Git. This folder is not an instruction to execute every stage again.
