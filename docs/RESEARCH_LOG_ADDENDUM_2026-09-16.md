# Living research-log addendum — 16 September 2026

**Purpose:** a version-controlled continuation entry for the designated `Solar_Flare_AIA_Research_Log_Updated_June_2026.docx`. The original Word document has not been opened or modified by this package. Retain `The full history.docx` as the archival correspondence record. Reconcile this dated entry with the living narrative log when its current file is edited; do not maintain contradictory status claims.

The original scientific aim remains Cycle-24-to-Cycle-25, same-region 48-hour M/X forecasting using temporal AIA, SHARP and planned past-only GOES, with optional magnetic images and a separate PINN/PIML track. The six-channel 96-minute-cadence archive and completed within-cycle benchmarks are preserved.

Original yearly manifests explain the three inspected obsolete embedded labels. Source headers distinguish nominal AIA record slots from actual exposure timing. A separate three-lag history-only candidate design was implemented rather than changing issue times or declaring the original snapshot runs corrected. The July 2024 mismatch remains explicitly recorded, including its distinct conditional label-window finding.

Archive inventory: 137,714 exact nonempty target objects of 141,644 requested. Temporal index: 118,217 nominal complete histories. Four real sequences and the AIA+SHARP forward/backward engineering path passed, without independent-test examples or forecasting scores. A broader Cycle-24 training proposal now includes later years, reserving entire calibration and threshold region components. It remains unfrozen.

Raw Cycle-24 SHARP arrays are built at `[64,725,3,15]`. All 70,217 requested unique source keys matched. Numerical incompleteness affects 206 candidate rows; explicit NOAA conflicts are zero in the build report. No quality filtering, fitted preprocessing, calibrator, threshold or scientific baseline training was performed by this build.

The immediate workflow is documentation consolidation in the single existing repo, followed by a specified CPU baseline run once applicable quality and label/timing eligibility are resolved. GPU image/fusion training is a later VM task, not Cloud Shell model production. Full GOES fusion, UQ/calibration results, optional HMI images and PINN remain unexecuted objectives, not silently dropped scope.

See `AIA_RESEARCH_STATUS_2026-09-16.md` and `AIA_DECISION_REGISTER_2026-09-16.md` for the recorded distinctions and run IDs. The internal combined proposal is retained privately and is not included in this public checkpoint. The earlier within-cycle experiments remain the baseline phase; the continuation is an extension, not their replacement.
