# Pull Command Note

To pull the separate PINN/PIML track files locally:

```bash
cd ~/solar_flare_aia
git pull
git log --oneline -10
```

Then begin implementation of:

```text
notebooks/training/20A_pinn_foundation_heat_equation_demo.ipynb
```
